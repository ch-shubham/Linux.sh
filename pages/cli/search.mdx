# Search

- All the commands are being saved by the terminal in the bash history.
- We can use tabs for the autocompletion of the directories. `cd <double tab for suggestions>`.
- `ctrl + r` for reverse search in shell. Continue pressing `ctrl + r` for more backward results.